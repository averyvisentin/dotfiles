# Required directories and files
music_directory             "/mnt/Seagate/Music"
playlist_directory          "~/.config/mpd/playlists"
db_file                     "~/.config/mpd/database"
log_file                    "~/.config/mpd/log"
pid_file                    "~/.config/mpd/pid"
state_file                  "~/.config/mpd/state"
sticker_file                "~/.config/mpd/sticker.sql"

# User and group to run as
# This should be your actual username and group.
user                        "avery" # <-- IMPORTANT: Change this
group                       "avery" # <-- IMPORTANT: Change this (often same as username)

# IPC Socket Connection
# This binds MPD to a Unix socket in your music directory as requested.

# Optional: Set permissions for the socket if other users need access
# unix_permissions            "0660"
